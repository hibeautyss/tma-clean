export const loadUserState = async (telegramId) => {
  console.info("loadUserState placeholder", { telegramId });
  return null;
};

export const saveUserState = async (telegramId, state) => {
  console.info("saveUserState placeholder", { telegramId, state });
  return true;
};
